{
  "name": "ourcourseville",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.273",
    "prettier": "^3.3.3",
    "prettier-plugin-organize-imports": "^4.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run ./src/index.js",
    "start": "bun run ./src/index.js",
    "build": "bun run tsc --noEmit && babel src --extensions '.ts,.js' --out-dir build && node copy-files.mjs"
  },
  "type": "module",
  "dependencies": {
    "@babel/cli": "^7.25.6",
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.4",
    "@babel/preset-typescript": "^7.24.7",
    "babel-plugin-add-import-extension": "^1.6.0"
  }
}
